# ✅ Critical Fix Applied

I have disabled the "App Manifest" feature in `nuxt.config.ts`. This was causing the `404 Not Found` error for `dev.json`.

## 🔄 Final Restart Required

1. **Stop** the frontend server (Ctrl+C).
2. **Run** `npm run dev` again.

This will PERMANENTLY fix the manifest error.

Login with:
- **Email:** `admin@agazi.edu.et`
- **Password:** `password123`
